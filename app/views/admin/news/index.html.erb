<h1>Notícias</h1>

<table class="table table-hover">
<!-- 
	<tr>
		<th align="left">Título</th>
		<th>Descrição</th>
		<th>Conteúdo</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>
-->

<% @news.each do |news| %>
  <tr>
    <td><%= news.title %></td>
	<!-- <td><%= news.description %></td>
    <td><%= simple_format(news.content) %></td> -->
    <td>
	<div class="btn-group">

	<a href="<%= admin_news_path(news) %>" class="btn btn-small">
		<i class="icon-zoom-in"></i> Visualizar
	</a> <!-- Exatamente o mesmo link só que escrito em HTML -->
	
	<!-- <%= link_to new_admin_news_path(news), class: 'btn btn-small' do %>
		<i class="icon-user"></i> Visualizar
	<% end %> -->w
    <%= link_to edit_admin_news_path(news), class: 'btn btn-small' do %>
		<i class="icon-edit"></i> Editar
	<% end %>
    <%= link_to admin_news_path(news), method: :delete, class: 'btn btn-small', data: { confirm: 'Are you sure?' } do %>
		<i class="icon-remove"></i> Deletar
	<% end %>
	</div>
	</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Adicionar Notícia', new_admin_news_path, class: 'btn btn-primary' %>
